                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapm                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapp                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapR                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapc                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map{                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapu                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapS                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapu                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapM                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapm                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapu                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapE                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapf                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapf                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapc                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapu                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapR                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapf                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map}                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapf                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapm                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map'                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapc                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map'                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map;                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapm                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapp                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map{                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapS                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maph                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapp                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapp                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapg                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapP                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapu                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapM                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapu                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapS                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapc                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maph                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapd                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapU                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maps                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapP                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapc                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapk                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapg                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maph                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapc                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapk                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapX                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapD                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapS                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapg                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapW                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapT                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapd                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapg                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapU                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapp                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapB                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maph                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map3                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapF                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapl                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapd                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapa                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapd                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapA                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapw                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapU                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapp                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapR                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapi                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapg                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maph                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapt                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map
                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapC                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.maph                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mape                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapv                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapr                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapD                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapo                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapw                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.mapn                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map,                                                  if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });

                                          const predictiveData = HISTORICAL_REVENUE.map                                                   if (e.dreClass === 'REVENUE' || cleanCat === 'ajuste de valor' || cleanCat === 'desconto concedido') {
                                                      const isRevenueClass = e.dreClass === 'REVENUE';
                                                      const val = isRevenueClass ? e.amount : -e.amount;
                                                      
                                                      // Update currentRealizedData sub-totals for consistency with DRE
                                                      currentRealizedData[mIdx].services += val;

                                                      // Breakdown only for the specific selected month modal
                                                      if (mIdx === currentMonthIndex) {
                                                          const key = isRevenueClass ? 'Receitas Diretas' : 'Ajustes';
                                                          if (!serviceBreakdown[key]) serviceBreakdown[key] = { name: key, realized: 0, forecast: 0, count: 0 };
                                                          serviceBreakdown[key].realized += val;
                                                          serviceBreakdown[key].count++;
                                                      }
                                                  }
                                              }
                                          });
